# Abolish PDFs

AbolishPDFS will convert PDF documents into high-fidelity HTML while preserving the defining qualities that make pdf2htmlEX valuable

We will make a few different choices from `pdf2htmlEx`. 
- We won't use Poppler
- We want to be cross-platform compatible
- We will still use FontForge