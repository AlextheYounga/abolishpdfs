# Tasks

## Implementation

- [ ] Define serializable object-scoped text failure reasons in the model.
- [ ] Record native-emission eligibility and semantic text availability during PDFium extraction.
- [ ] Add document-level integrity validation before output file creation.
- [ ] Propagate structured failures through the CLI with page and object context.
- [ ] Update diagnostic JSON and fixture expectations for native text and explicit failures.
- [ ] Add regression tests proving raster graphics do not satisfy the native-text requirement.

## Validation

- [ ] Run focused model, output, and CLI failure tests.
- [ ] Verify successful fixtures contain expected native DOM text.
- [ ] Verify unsupported-text fixtures fail visibly and do not leave successful output artifacts.
- [ ] Run formatting, all Rust tests, clippy, and diff checks.

## Completion

- [ ] Review all fallback paths for silent text success.
- [ ] Record fixture behavior and validation results in completion notes.

## Completion notes

Not started.
